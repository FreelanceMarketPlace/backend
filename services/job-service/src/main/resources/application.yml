spring:
  application:
    name: job-service

server:
  port: 8082

management:
  endpoints:
    web:
      exposure:
        include: health,info

# spring:
#   data:
#     mongodb:
#       uri: ${MONGODB_URI:mongodb://localhost:27017/nhom611}
